import { UniqueColumnPipe } from './unique-column.pipe';

describe('UniqueColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
